name: Academy build verification
on:
  push:
    branches: [rebuild-flashcard-academy]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build
